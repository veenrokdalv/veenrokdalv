import logging


utils = logging.getLogger('accounts.utils')
services = logging.getLogger('accounts.services')

auth_backends = logging.getLogger('accounts.auth.backends')

auth_views = logging.getLogger('accounts.auth.views')

auth_forms = logging.getLogger('accounts.auth.forms')
